export interface CategoryType {
  name: string;
  id: string;
  imageUrl: string | null;
}

export interface SubCategoryType {
  name: string;
  id: string;
  imageUrl: string | null;
  //   category: string;
}
